---
-   name: Provision Site
    hosts: server
    remote_user: lera
    become: true
    become_method: sudo
    vars:
        nginx_conf: api.conf
        remote_nginx_conf: /etc/nginx/conf.d/api.conf
    roles:
        - docker
        - nginx